scalar GraphQLDate


type Place {
  _id: ID!
  id: Int!
  name: String!
  description: String!
  review: String
  rating: Int
  created: GraphQLDate!
}

input PlaceInputs {
  name: String!
 
}

##### Top level declarations

type Query {
  about: String!
  placeList: [Place!]!
}

type Mutation {
  setAboutMessage(message: String!): String
  placeAdd(place: PlaceInputs!): Place!
  addToFavlist(nameInput: String!): String
}
